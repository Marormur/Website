# Normalize line endings cross-platform
# Store LF in the repository; checkout CRLF on Windows when appropriate
* text=auto

# Keep UNIX line endings for shell scripts and web assets to avoid MIME-Type issues
*.sh text eol=lf
*.bash text eol=lf
*.zsh text eol=lf
*.css text eol=lf charset=utf-8
*.js text eol=lf charset=utf-8
*.html text eol=lf charset=utf-8
*.json text eol=lf charset=utf-8
*.md text eol=lf charset=utf-8

# Treat common binary assets as binary (no EOL conversion)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.zip binary
